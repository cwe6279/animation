# Core rendering & audio
# pygame-ce is a drop-in fork with wheels for new Python releases (3.13/3.14);
# plain `pygame` also works on Python <= 3.12.
pygame-ce>=2.5.0
pyaudio>=0.2.13
numpy>=1.24.0

# Streaming TTS with word timestamps (default backend; needs internet)
edge-tts>=7.0.0
# edge-tts serves MP3, decoded on the fly by ffmpeg. Install ffmpeg system-wide
# (brew/apt install ffmpeg) or uncomment for a bundled binary:
# imageio-ffmpeg>=0.5.1

# Phoneme accuracy (optional; regex fallback is used automatically without it).
# Its NLTK data (cmudict, tagger) is downloaded on first run.
g2p-en>=2.1.0

# Optional: cloud TTS with lower latency + raw PCM (set ELEVENLABS_API_KEY)
# (no extra package: uses aiohttp, already pulled in by edge-tts)

# Voice round trip (voice_loop.py): mic -> STT -> Claude -> face
anthropic>=1.0
vosk>=0.3.45            # local streaming speech-to-text (small model auto-downloads, ~40 MB)
# faster-whisper>=1.0   # optional, more accurate STT (--stt whisper), slower per turn

# Optional: OpenAI as the brain or as speech-to-text, for comparison (--llm openai, --stt openai)
openai>=1.0

# Optional: vision (voice_loop.py --camera ...): camera capture for scene notes
opencv-python-headless>=4.9

# Dev
pytest>=8.0
